# `{{FOLDER}}/`

{{BLURB}}

This README is not a note: it carries no frontmatter, and `bin/check.mjs` and `bin/index.mjs`
both skip it. It is here so the folder exists in a fresh clone and explains itself. Delete it
once the folder has real notes in it, or keep it and add local conventions.

See `../AGENTS.md` for the note contract and `../START-HERE.md` for the tour.
